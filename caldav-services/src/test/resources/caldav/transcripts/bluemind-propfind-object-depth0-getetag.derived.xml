<?xml version="1.0" encoding="UTF-8"?>
<!--
  DERIVED, not captured: no authenticated Depth:0 PROPFIND of getetag on a
  BlueMind .ics href is in the corpus yet (EXO-90307, review F7; the capture
  script dev/golden-capture/capture-bluemind-object-etags.sh records it).
  Shape from BlueMind's source (bluemind-public master):
  - plugins/net.bluemind.dav.server/src/net/bluemind/dav/server/proto/propfind/PropFindProtocol.java:66-70,99-112
    execute() resolves ds.from(query.getPath()) and addChildren(dr, ZERO);
    getOneLevel() answers one response, href = dr.getPath(), status 200,
    the getetag value fetched by GetTag;
  - plugins/net.bluemind.dav.server/src/net/bluemind/dav/server/store/DavStore.java:292-318,355-357
    from() picks the ResType whose regex matches the path (VSTUFF for a
    .ics), Depth ZERO adds only that node;
  - plugins/net.bluemind.dav.server/src/net/bluemind/dav/server/proto/props/webdav/GetTag.java:46-56
    on a VEventNode ("unsupported ressource type", timestamp 0) the value is
    SyncTokens.get(dr.getPath(), 0): the RAW token bmdav_<lnum>_0, unquoted,
    lnum = path.hashCode() + Integer.MAX_VALUE (SyncTokens.java:39-47) - the
    lnum below is ILLUSTRATIVE, copied from the derived listing fixture so
    the two fixtures name one object, not the hash of the href they carry;
  - plugins/net.bluemind.dav.server/src/net/bluemind/dav/server/xml/MultiStatusBuilder.java:36-49,63-79
    d:multistatus with the cal/rd/cso/aic/me namespace attributes (the
    constructor), d:response / d:href / d:propstat / d:status / d:prop
    (newResponse).
  THE SAME DOCUMENT IS WHAT A MISSING OBJECT GETS: MethodRouter.java:162-175
  answers 404 only when DavStore.existingResource() says no, and that method
  falls to its default "assume yes" for a VSTUFF node (DavStore.java:474-502);
  ResType.VSTUFF.from() mints new VEventNode(path) with no lookup
  (ResType.java, VSTUFF entry). So this read answers a version, never
  existence - which is why BlueMindImportWriter asks it only after the
  one-href multiget affirmed presence.
  The lnum is the one the derived Depth:1 listing fixture gives the same
  href (bluemind-propfind-collection-depth1-items.derived.xml): the two are
  equal iff the request path string equals containerPath + uid + ".ics" as
  DavStore.addEvents builds it (DavStore.java:402) - the spelling hypothesis
  the writer verifies live, once per collection, before trusting this read.
-->
<d:multistatus xmlns:d="DAV:" xmlns:cal="urn:ietf:params:xml:ns:caldav" xmlns:rd="urn:ietf:params:xml:ns:carddav" xmlns:cso="http://calendarserver.org/ns/" xmlns:aic="http://apple.com/ns/ical/" xmlns:me="http://me.com/_namespace/"><d:response><d:href>/dav/calendars/__uids__/9F3C1A20-4D5E-4B7A-8C61-2E0D7A4B9C13/calendar:Default:9F3C1A20-4D5E-4B7A-8C61-2E0D7A4B9C13/evt-1.ics</d:href><d:propstat><d:status>HTTP/1.1 200 OK</d:status><d:prop><d:getetag>bmdav_851210693_0</d:getetag></d:prop></d:propstat></d:response></d:multistatus>
