<?xml version="1.0" encoding="UTF-8"?>
<!--
  DERIVED, not captured: no authenticated one-href calendar-multiget REPORT
  against a BlueMind calendar is in the corpus yet (EXO-90307, review F7; the
  capture script dev/golden-capture/capture-bluemind-object-etags.sh records
  it). Shape from BlueMind's source (bluemind-public master):
  - plugins/net.bluemind.dav.server/src/net/bluemind/dav/server/proto/report/caldav/CalendarMultigetExecutor.java:64-88
    execute() takes each href's last segment without ".ics" as the item uid
    (URL-decoded) and asks ICalendar.multipleGet(extIds): only the items the
    store holds come back;
  - CalendarMultigetExecutor.java:114-130
    write() emits ONE d:response per returned item, href =
    <REPORT request path> + uid + ".ics", status 200, and for getetag the
    value SyncTokens.getEtag(icsPath, serie.version): the token
    bmdav_<lnum>_<version> BASE64-ENCODED AND QUOTED (SyncTokens.java:78-83)
    - a different string from the raw bmdav_<lnum>_0 the Depth:1 listing
    publishes for the same object, and the reason this channel answers
    presence for the import door but never the version it records;
  - plugins/net.bluemind.dav.server/src/net/bluemind/dav/server/xml/MultiStatusBuilder.java:36-49,63-79
    the multistatus envelope (the constructor) and the response element
    (newResponse).
  The value below is the quoted base64 of "bmdav_851210693_127" (the derived
  listing's lnum for evt-1.ics, at an arbitrary item version 127).
-->
<d:multistatus xmlns:d="DAV:" xmlns:cal="urn:ietf:params:xml:ns:caldav" xmlns:rd="urn:ietf:params:xml:ns:carddav" xmlns:cso="http://calendarserver.org/ns/" xmlns:aic="http://apple.com/ns/ical/" xmlns:me="http://me.com/_namespace/"><d:response><d:href>/dav/calendars/__uids__/9F3C1A20-4D5E-4B7A-8C61-2E0D7A4B9C13/calendar:Default:9F3C1A20-4D5E-4B7A-8C61-2E0D7A4B9C13/evt-1.ics</d:href><d:propstat><d:status>HTTP/1.1 200 OK</d:status><d:prop><d:getetag>"Ym1kYXZfODUxMjEwNjkzXzEyNw=="</d:getetag></d:prop></d:propstat></d:response></d:multistatus>
